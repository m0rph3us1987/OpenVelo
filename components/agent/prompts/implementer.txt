Implement the plan you wrote at `{{PLAN_PATH}}` in the previous turn.

The SKILLS, architecture conventions, and story from the planning turn are already in your conversation history — do not reload them.

Before coding, call `todowrite` once with the list of logical units you intend to do (one per top-level `## N.` section of the plan, per file, or per coherent step — pick the granularity that makes your progress legible). All items start with `status: "pending"`. As you work, call `todowrite` again each time you switch tasks: mark the finished item `completed`, set the next item to `in_progress` (only one at a time). The web-UI renders this panel; populating it is required.