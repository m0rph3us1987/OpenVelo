{
  "name": "openvelo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix components/web-ui",
    "postinstall": "npm install --prefix components/agent && npm install --prefix components/orchestrator && npm install --prefix components/web-ui",
    "build": "npm run build --prefix components/orchestrator && npm run build --prefix components/web-ui",
    "production": "npm run start --prefix components/web-ui",
    "docker-image-linux": "docker build -t openvelo-agent:linux components/agent",
    "docker-image-linux-no-cache": "docker build --no-cache -t openvelo-agent:linux components/agent",
    "docker-image-webui-linux": "docker build -t openvelo-webui:linux components/web-ui",
    "docker-image-orch-linux": "docker build -t openvelo-orchestrator:linux components/orchestrator",
    "docker-build-all-linux": "npm run docker-image-linux && npm run docker-image-webui-linux && npm run docker-image-orch-linux",
    "test": "npm run test --prefix components/web-ui"
  }
}
